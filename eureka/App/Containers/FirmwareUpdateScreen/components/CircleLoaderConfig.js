/**
 * Duration of one animation step
 * @type {number}
 */
export const ANIMATION_DURATION = 500;
/**
 * Average time needed for finishing animation. Usually animation is in progress (in average it may take 300 ms to finish)
 * + 1 animation is needed to finish to 100%.
 * @type {number}
 */
export const ANIMATION_TO_FINISH = ANIMATION_DURATION + 300;
